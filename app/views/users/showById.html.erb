<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Show Articles</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 40px;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
    }

    .article-container {
      background-color: #ffffff;
      padding: 30px 40px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      max-width: 700px;
      width: 100%;
    }

    h1 {
      color: #333;
      margin-bottom: 30px;
      font-size: 28px;
      text-align: center;
    }

    .article-card {
      border: 1px solid #ddd;
      border-radius: 6px;
      padding: 20px;
      margin-bottom: 20px;
      background-color: #fafafa;
    }

    .article-detail {
      margin-bottom: 12px;
    }

    .article-detail strong {
      display: inline-block;
      width: 100px;
      color: #007bff;
      font-size: 16px;
    }

    .article-detail span {
      font-size: 16px;
      color: #555;
    }

    .button-group {
      display: flex;
      justify-content: flex-start;
      gap: 10px;
      margin-top: 15px;
      flex-wrap: wrap;
    }

    .btn {
      padding: 8px 16px;
      border-radius: 5px;
      font-size: 14px;
      text-decoration: none;
      color: white;
      border: none;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .btn-show {
      background-color: #17a2b8;
    }

    .btn-show:hover {
      background-color: #117a8b;
    }

    .btn-edit {
      background-color: #28a745;
    }

    .btn-edit:hover {
      background-color: #218838;
    }

    .btn-delete {
      background-color: #dc3545;
    }

    .btn-delete:hover {
      background-color: #c82333;
    }

    .no-articles {
      text-align: center;
      font-style: italic;
      color: #888;
      margin-top: 20px;
    }

    form {
      display: inline;
    }
  </style>
</head>

<body>
  <div class="article-container">
    <h1>Show articles related to <%= @user.username %></h1>

    <% if @article.any? %>
      <% @article.each do |article| %>
        <div class="article-card">
          <div class="article-detail">
            <strong>Title:</strong> <span><%= article.title %></span>
          </div>
          <div class="article-detail">
            <strong>Description:</strong> <span><%= article.description %></span>
          </div>

          <div class="button-group">
            <%= link_to 'Show', "/showById/#{article.id}", class: 'btn btn-show' %>
            <%= link_to 'Edit', "/edit/#{article.id}", class: 'btn btn-edit' %>
            <%= button_to 'Delete', "/delete/#{article.id}", method: :delete, data: { turbo_confirm: 'Are you sure you want to delete this article?' }, class: 'btn btn-delete' %>
          </div>
        </div>
      <% end %>
    <% else %>
      <p class="no-articles">No articles found for this user.</p>
    <% end %>
  </div>
</body>
</html>
