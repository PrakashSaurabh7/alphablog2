<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Show Article</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 40px;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .article-container {
      background-color: #ffffff;
      padding: 30px 40px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      width: 100%;
      text-align: center;
    }

    h1 {
      color: #333;
      margin-bottom: 30px;
      font-size: 28px;
    }

    .article-detail {
      margin-bottom: 20px;
      text-align: left;
    }

    .article-detail strong {
      display: inline-block;
      width: 120px;
      color: #007bff;
      font-size: 18px;
    }

    .article-detail span {
      font-size: 18px;
      color: #555;
    }

    .button-group {
      margin-top: 30px;
      display: flex;
      justify-content: center;
      gap: 12px;
      flex-wrap: wrap;
    }

    .btn {
      padding: 10px 20px;
      border-radius: 5px;
      font-size: 16px;
      text-decoration: none;
      color: white;
      background-color: #007bff;
      border: none;
      cursor: pointer;
      transition: background-color 0.3s ease;
      min-width: 120px;
      text-align: center;
    }

    .btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>

<body>
  <div class="article-container">
    <h1>Show Article</h1>

    <div class="article-detail">
      <strong>Title:</strong> <span><%= @article.title %></span>
    </div>

    <div class="article-detail">
      <strong>Description:</strong> <span><%= @article.description %></span>
    </div>

    <div class="button-group">
      <%= link_to "Edit", "/edit/#{@article.id}", class: "btn" %>
      <%= button_to "Delete", "/delete/#{@article.id}", method: :delete, data: { turbo_confirm: "Are you sure you want to delete this article?" }, class: "btn" %>
      <%= link_to "Return", "/", class: "btn" %>
    </div>
  </div>
</body>
</html>
